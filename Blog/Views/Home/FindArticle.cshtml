@model ICollection<Blog.Models.Post>
@{
    Layout = "_Layout";
    ViewData["Title"] = "Find Article";
}

@foreach (var post in Model)
{
    <h2><a asp-controller="Home" asp-action="Read" asp-route-id="@post.Id">@post.Title</a></h2>
}

